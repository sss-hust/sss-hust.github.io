---
title: GPTQ
date: 2026-03-07 21:20:50
categories:
- AI与深度学习
tags:
- GPTQ
- 模型量化
- 网络剪枝
- Hessian矩阵
- OBS
---
[toc]

整体发展历程为OBD-OBS-OBC-OBQ-GPTQ

## OBD&OBS

对剪枝算法进行建模，剪枝就是为了把一些参数置为0，同时最小程度影响模型的能力，数学表现为损失变化最小，当然，剪枝的前提是训练已经收敛到极点了，数学表现为Hessian特征值全为负数，收敛于极小值点。

> Hessian为目标变量关于所有自变量的二阶导，位置(i,j)表示y对$x_i,x_j$求二阶导

通过数学展开，我们可以发现，要让损失值尽可能小，根据如下公式（这里的W已经打平成列向量了）以及收敛时一阶导数的性质：

$$
L(W + \delta W) = L(W) + \left(\frac{\partial L}{\partial W}\right)^T \delta W + \frac{1}{2} \delta W^T H \delta W + \mathcal{O}(\delta W^3)
$$

得到损失值为：$\delta L \approx \frac{1}{2} \delta W^T H \delta W$，于是我们的目标就变成了找到最小的W变化量，让损失值变化最小。

OBD做出强假设：海森矩阵是对角矩阵，多个参数剪枝结果等于单个参数剪枝结果的和。

OBS在此基础上放弃强假设，进一步展开研究，算法大致思想为贪心搜索，每次都去寻找对整体影响最小的参数，将其置为0，直到满足要求，数学公式就是去量化这个“影响”。

## OBC

对损失函数进行更改，不在原有的域内找答案，采用新的损失函数，对应新的方法。将原始问题拆分成每一行单独处理
